# 🚀 Task Summarization Experiment (tasks4)

This project demonstrates the use of the **OpenAI Chat Completions API** to summarize paragraph-length task descriptions into short, concise phrases. It utilizes the `uv` package manager and is structured as a Python source package (`src/tasks4`).

---

## ⚙️ Setup and Execution Instructions

Follow these steps to ensure the project environment is configured and the script can connect to the OpenAI API.

### 1. Install Dependencies

Ensure you are in the project's root directory (the folder containing `pyproject.toml`) and use `uv sync` to install the `openai` library and set up the local virtual environment (`.venv`).

```bash
uv sync
